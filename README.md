# 📰 Mobile News App (Expo + React Native)

Мобильное приложение для чтения новостей с возможностью:  
- просмотра новостей по категориям,  
- открытия детальной страницы новости,  
- добавления статей в избранное,  
- получения уведомлений о новых статьях.  

Разработано с использованием **Expo**, **React Native** и **Redux Toolkit (RTK Query)**.

---

## 🚀 Технологический стек

**Основные технологии:**  
- **React Native** — кроссплатформенная мобильная разработка  
- **Expo** — среда разработки и сборки  
- **Expo Router** — файловая навигация  
- **Redux Toolkit + RTK Query** — глобальное состояние и работа с API  
- **TypeScript** — строгая типизация  

**Дополнительно:**  
- `react-native-svg` — SVG иконки  
- `expo-notifications` — push-уведомления  
- `react-native-webview` — открытие оригинальных новостей  
- `AsyncStorage` — локальное хранение данных  

---

## 📂 Структура проекта

```txt
app/
 ├─ index.tsx              # Главный экран (новости)
 ├─ login.tsx              # Экран авторизации
 ├─ _layout.tsx            # Навигация Expo Router
 ├─ notifications/         # Экран уведомлений
 ├─ news/                  # Детальная страница новости
 ├─ favorites/             # Избранные новости

components/
 ├─ NewsCard.tsx           # Карточка новости
 └─ ui/                    # UI-компоненты

store/
 ├─ services/
 │   └─ newsApi.ts         # API новостей (RTK Query)
 ├─ currentArticleSlice.ts # Текущая статья
 ├─ notificationsSlice.ts  # Уведомления
 └─ index.ts               # Redux store

types/
 ├─ news.ts                # Типы новостей
 └─ notification.ts        # Типы уведомлений

assets/                    # Иконки и изображения

## 🚀 Как запустить приложение

1. **Клонируйте репозиторий**  
```bash
git clone <URL_вашего_репозитория>
cd <имя_папки_проекта>
2.Установите зависимости
npm install
3.Запустите приложение
npx expo start -c --tunnel
4.Скачать приложение для телефона
Установите приложение Expo Go (iOS или Android).
Отсканируйте QR-код, который появится в терминале или браузере, чтобы открыть приложение на вашем устройстве.

## 🧠 Ключевые экраны и файлы

### app/index.tsx — Главный экран
- Загружает новости через API (RTK Query)  
- Реализует поиск, фильтрацию и пагинацию  
- Создает уведомления о новых статьях  
- Отображает список новостей через `NewsCard`  
- Навигация: избранное, уведомления, детальная страница  

### services/newsApi.ts
- Работа с API новостей (GNews) через RTK Query  
- Кэширование и обработка состояний загрузки/ошибок  
- Преобразует данные API в единый формат для приложения  

### app/notifications/index.tsx
- Экран уведомлений  
- Получает уведомления из Redux  
- Отображает их в `FlatList` с красной точкой для непрочитанных  
- Анимация точки через `Animated.View`  
- При нажатии на уведомление точка исчезает  
- Пустой экран, если уведомлений нет  
- Хедер с кнопками выхода и настроек  

### favorites/index.tsx
- Экран "Избранное"  
- Подключение `favorites-context` для управления избранным  
- Поиск и фильтрация по заголовку и категориям  
- Список отображается в `FlatList`  
- Карточка статьи содержит: картинку, заголовок, дату, кнопку удаления  
- Навигация на детальную страницу через `router.push`  
- Пустой список отображает текст «Пока ничего не добавлено 🔖»  

### news/[id].tsx
- Экран детальной новости  
- Получение статьи из Redux (`currentArticleSlice`)  
- Отображает изображение, заголовок, описание, дату и рейтинг  
- Кнопка "В избранное" добавляет статью в `favorites-context`  
- Кнопка "Открыть оригинал" открывает WebView  
- Обработка загрузки и ошибок через RTK Query  

### app/login.tsx
- Экран авторизации  
- Вход через логин/пароль или биометрию (Face ID / Touch ID)  
- Проверка логина: `admin / 1234`  
- После успешного входа вызывается `login()` из контекста и переход на главный экран  
- При ошибке отображается `Alert`  

### hooks/useBiometricAuth.ts
- Проверка и запуск биометрической аутентификации  
- Используется в `login.tsx`  
- Возвращает `true`, если авторизация успешна  

### store/slices/notificationsSlice.ts
- Управление уведомлениями в Redux  
- Хранит массив уведомлений с полем `unread`  
- Действия:
  - `addNotifications` — добавление новых уведомлений без дублирования  
  - `markAsRead` — отметка уведомления как прочитанного  
- Используется через `useSelector` и `dispatch`  

---

### 📝 Резюме
Приложение построено по архитектуре **Smart Container + Presentational Components**, где:  
- **Smart Container:** `app/index.tsx`, `favorites/index.tsx`, `notifications/index.tsx`  
- **Presentational Components:** `NewsCard`, UI-компоненты  

Используется комбинация **Redux + Context API** для управления глобальным и локальным состоянием.  
Все экраны адаптированы под мобильный UX, включая поиск, фильтры, избранное и уведомления.
